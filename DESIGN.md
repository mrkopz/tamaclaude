# Tamaclaude (CYD) — ข้อตกลงการออกแบบ

อุปกรณ์ตั้งโต๊ะที่แสดงสถานะ Claude Code session แบบเรียลไทม์ผ่านมาสคอต Claude
ฮาร์ดแวร์: ESP32-2432S028R ("Cheap Yellow Display")

## ตัดสินใจแล้ว

| หัวข้อ | ผล |
|---|---|
| Firmware | ESP-IDF เขียนใหม่ทั้งหมด (ไม่ fork upstream) |
| Transport | BLE (NimBLE) เท่านั้นใน v1 — แยกเป็น interface เผื่อเติม WiFi ทีหลัง |
| จอ | ILI9341 320x240 แนวนอน |
| กราฟิกสแตก | LVGL v9 + custom draw widget 1 ตัวต่อ 1 slot |
| มาสคอต | ตัวละคร Claude สีส้ม `#D97757` บล็อคสี่เหลี่ยม |
| ผลิตกราฟิก | สคริปต์ Python generate แบบ procedural |
| รูปแบบ asset | rect list `{x,y,w,h,color}` — ไม่มีบิตแมป ไม่มี sprite pipeline |
| สื่อสถานะ | props (ตามเครื่องมือ) + eye state/ลำตัว (ตามอารมณ์) แยก 2 มิติ |
| ฝั่ง Mac | Swift binary เดียว: `--hook` / `--daemon` / menu bar app |
| tool → animation | daemon แปล (config file แก้ได้) firmware รู้แค่ enum คงที่ |
| โปรโตคอล | JSON snapshot ก้อนเดียว เขียนเข้า GATT characteristic |
| สถานะว่าง | มาสคอตนอน + นาฬิกา (ความสว่างคงที่ — ไม่หรี่อัตโนมัติ) |
| BLE หลุด | มาสคอตเปลี่ยนเป็นสีเทา (ไม่มีไอคอน ไม่มีข้อความ) |
| เกณฑ์เตือน | `Notification`, `StopFailure`, และ `Stop` ที่เงียบเกิน ~45 วิ |
| Slot | สูงสุด 3 ระยะห่างคงที่ 106px ยกทั้งกลุ่มไว้กึ่งกลาง เกินนับ "+N" แต่ alert ยังทะลุถึงได้ |
| Dev loop | Python เรนเดอร์จอทั้งใบเป็น PNG/GIF · layout constants gen จากไฟล์เดียว |
| โควตา | `rate_limits` จาก stdin ของ statusline — ไม่มี credential ไม่ยิงเน็ต |
| พื้นที่ล่าง | ลำดับ: การ์ด > โควตา > นาฬิกาใหญ่ · การ์ดสูงสุด 2 ใบ ที่เหลือเป็น "+N more" |
| ท้องฟ้า | เต็มจอใต้แถบบน: ฟ้า 22..119 + พื้นดิน 120..240 (ครึ่งจอ) · กอหญ้าเกาะเส้นขอบฟ้าแถวเดียว |
| ช่วงเวลา | 4 ช่วงกระโดดที่ขอบ: night 19–05 · dawn 05–07 · day 07–17 · dusk 17–19 |
| แหล่งเวลา | parse ชั่วโมงจาก `snapshot.clock` ที่มีอยู่แล้ว — ไม่มีอะไรเพิ่มบนสาย BLE |
| ฟ้าตอนหลุดลิงก์ | ไม่วาดเลย กลับเป็นพื้น `bg` เรียบ — clock ที่ค้างอยู่ไม่ใช่เวลาจริงอีกต่อไป |

## v1 scope

**อยู่ใน**: hooks → daemon → BLE → มาสคอตขยับตามเครื่องมือ · multi-session 3 ตัว ·
notification card · auto-reconnect · RGB LED กระพริบ · macOS menu bar app · config ผ่าน BLE ·
นาฬิกา sync จาก host

**ไม่อยู่ใน**: simulator SDL2 · touch · เสียง · microSD · WiFi · OTA

## ตัดสินใจเชิงเทคนิคที่ผมเลือกเอง

- **NimBLE** ไม่ใช่ Bluedroid — เล็กกว่า และเราต้องการแค่ BLE peripheral
- **ไม่มี OTA ใน v1** — partition เป็น factory app ก้อนเดียว + NVS ได้พื้นที่ app เต็ม ~3.5MB
  จาก 4MB (มี OTA จะเหลือ slot ละ ~1.7MB) แฟลชผ่าน USB อยู่แล้วตอนพัฒนา
- **LVGL partial buffer** 2 ก้อน ก้อนละ 1/10 จอ (~15KB) ไม่ใช่ full framebuffer 150KB
- **GATT**: 3 characteristic — `state` (write), `config` (read/write), `event` (notify, สงวนไว้ใช้ v2)
- **daemon truncate ทุก string** ก่อนส่ง ให้ snapshot พอดี 1 MTU (517) — ไม่มี chunking

## สิ่งที่เปลี่ยนหลังเห็นภาพจริง

ทั้งหมดนี้มาจากการเรนเดอร์จอ 320x240 แล้วดู ไม่ได้มาจากการคิดเอา

- **สัดส่วนมาสคอตหดลง** — เดิมกว้าง 24 unit กินเต็ม slot 80px จน prop ที่ถืออยู่
  ไปตกบนพื้นหลังของ slot ถัดไป แล้วอ่านผิดว่าเป็นของตัวข้างๆ
  ตอนนี้ซิลลูเอ็ต 16 unit + พื้นที่ prop ถึง 22 unit = 67.5px เหลือขอบ 6px ต่อข้าง
- **เลิกสลับสีพื้นหลัง slot** — มันคือต้นเหตุที่ทำให้ prop ดูเป็นของ slot ข้างๆ ใช้เส้นคั่นบางแทน
- **`waiting` เปลี่ยนจาก "!" แดง เป็น "?" เหลือง** — เดิมซ้ำกับ `alert` จนแยกไม่ออก
  ทั้งที่ความหมายคนละเรื่อง ("รอคุณตอบ" ไม่ใช่ "พัง") ต่างทั้งรูปทรงและสี
- **notification เป็นสแต็ก 3 ใบ ไม่ใช่ใบเดียว** — พื้นที่ล่างว่างเปล่าอยู่แล้ว และตรงกับ
  ข้อตกลงว่า session ที่เกินจำนวน slot ต้องส่งการเตือนทะลุถึงผู้ใช้ได้
- **นาฬิกาใหญ่กลางจอตอนไม่มีการเตือน** — เดิมมีแค่นาฬิกาเล็กบนแถบ ทำให้ 2/3 ของจอ
  ว่างเปล่าและดูเหมือนอุปกรณ์พัง ทั้งที่นี่คือสภาพที่จอเป็นอยู่เกือบตลอดเวลา
- **แถบมาสคอตเตี้ยลงจาก 128px เหลือ 86px** — มาสคอตสูงแค่ ~50px ที่เหลือยกให้ card
- **ลูกโลกเปลี่ยนจากสี่เหลี่ยมกลวงเป็นทรงกลมตัน** — ของเดิมเป็นสี่เหลี่ยมกลวงเหมือน
  แว่นขยายเป๊ะ ที่ 12px แยกไม่ออก ตอนนี้เป็นลูกกลมมีทวีปเลื่อน
- **ลูกโลกย้ายขึ้นไปอยู่บนหัวและโตขึ้นเป็น 7.2 unit** — ตอนถืออยู่ข้างตัวมันเป็นแค่ก้อนกลม
  เล็กๆ ที่ริมกรอบ อ่านเป็นของที่วางอยู่ข้างๆ ไม่ใช่สิ่งที่มาสคอตกำลังทำ ตอนนี้คร่อมกลางหัว
  (ทับยอดหัวได้ แต่หยุดเหนือตา) จึงเป็นชิ้นที่สายตาเห็นก่อนอย่างอื่นใน slot
  ทรงกลมประกอบจากแถบแนวนอน 14 แถบ (แถบละ 0.5 unit = 2px) ที่กว้างตามสมการวงกลม —
  มุมตัดชุดเดียวแบบเดิมพอไหวตอนเส้นผ่านศูนย์กลาง 5 unit แต่พอโตขึ้นมันอ่านเป็นแปดเหลี่ยม
  วัดความกว้างที่ *กึ่งกลาง* แถบ ไม่ใช่ขอบแถบ และปัดขอบเป็นพิกเซลเต็ม — วัดที่ขอบทำให้
  แถบหัวท้ายกว้างเกินจริงจนลูกโลกอ่านเป็นโดมแบน ส่วนแถบละ 4px ให้บันไดหยาบเกินกว่าจะกลม
- **ลูกโลกลอยนิ่ง ไม่เด้งขึ้นลงตามตัว** — prop ชิ้นอื่นเลื่อนตาม `dy` ของลำตัวเพราะมันเล็ก
  และอยู่ข้างตัว ถ้าค้างที่เดิมจะอ่านเป็นหลุดจากมือ แต่ลูกโลกใหญ่และคร่อมหัวอยู่แล้ว
  พอเด้งตามตัวมันอ่านเป็นก้อนที่ติดหัว ตอนนี้ตัวเด้งผ่านลูกที่ลอยนิ่ง จึงอ่านเป็นลูกโลก
  ที่ลอยอยู่จริง และการหมุนของทวีปเด่นขึ้นเพราะไม่มีการเคลื่อนไหวอื่นมาแย่ง
- **ลูกโลกเป็นฟ้า ทวีปเป็นเขียว** — เดิมเป็นเหลืองอำพันกับทวีปสีหมึก ซึ่งอ่านเป็นลูกบอล
  หรือเหรียญพอๆ กับเป็นโลก คู่ฟ้า/เขียวคือรหัสสีที่ไม่ต้องอธิบาย และตัดกับตัวสีดินชัด
  ทวีปที่ล้นขอบถูกเล็มทิ้งเฉพาะส่วนที่ล้น ไม่ใช่ซ่อนทั้งก้อน — ก้อนที่หายวับอ่านเป็นกะพริบ
  ส่วนก้อนที่ค่อยๆ โผล่จากขอบอ่านเป็นแผ่นดินที่หมุนอ้อมมาจากอีกด้าน
- **`celebrate` ตาเปลี่ยนจากขีดแบนเป็น `^ ^`** — ขีดแบนซ้ำกับตาหลับของ `sleeping`
- **เพิ่มการกะพริบตา** — ท่ายืนเฉยๆ เดิมดูเป็นรูปนิ่ง กะพริบต้องเดินด้วยนาฬิกาที่ช้ากว่า
  ลูปอนิเมชัน (`cycle` แยกจาก `phase`) ไม่งั้นจะกะพริบทุกวินาทีแล้วดูกระวนกระวาย
  ตอนนี้กะพริบ 1 เฟรมทุก 4 ลูป ≈ ทุก 4.3 วินาที
- **slot ยกทั้งกลุ่มมาไว้กึ่งกลาง แทนที่จะอัดชิดซ้าย** — เดิมล็อกพิกัดสัมบูรณ์ไว้ด้วยเหตุผลว่า
  "slot ต้องนิ่ง ไม่งั้นจำไม่ได้ว่าตัวไหนคือ terminal ไหน" ซึ่งตีความแคบเกินไป
  สิ่งที่ต้องนิ่งจริงๆ คือ **ลำดับซ้าย→ขวา** ไม่ใช่พิกัด การจัดกลุ่มกึ่งกลางไม่เคยสลับลำดับ
  มีแค่ทั้งกลุ่มเลื่อนพร้อมกัน และมีป้ายชื่อโปรเจกต์กำกับอยู่แล้ว
  ระยะห่างยังคงที่ 80px เสมอ ไม่ยืดหดตามจำนวน
- **แลก slot ที่ 4 กับมาสคอตที่ใหญ่ขึ้น 33%** — 4 ตัวบนจอ 320px แปลว่าตัวละไม่เกิน 80px
  ซึ่งเล็กเกินกว่าจะดูออกจากอีกฝั่งโต๊ะ ทั้งที่ session ที่ 4 เป็นของหายากและถูกนับ "+N"
  ให้อยู่แล้ว เหลือ 3 ตัว → slot 106px → `unit_px` 3 → 4 (มาสคอต 67.5 → 90px)
  4 คือค่าสูงสุดที่ยังวาง 3 ตัวเรียงได้ และ outline 0.34 unit ยังปัดเป็น 1px เท่าเดิม
- **แถบโควตาสองชั้นแทนสามชั้น** — เดิม เลข % → แถบ → "Resets in ..." สูง 56px ต่อแถว
  ย้ายเวลารีเซ็ตขึ้นไปบรรทัดเดียวกับเลข % เหลือ 40px ต่อแถว โดยเลข % ยังฟอนต์ 24
  ซึ่งเป็นเหตุผลเดียวที่แถวนี้มีอยู่ แผงที่เตี้ยกว่าพื้นที่จึงจัดกลางแนวตั้ง
  ไม่ชิดบน — ก้นจอที่โล่งเป็นแถบอ่านเป็น "ของหาย" ไม่ใช่การตัดสินใจ
- **เพิ่มท่าเปลี่ยนผ่าน `entering` / `leaving`** — ตกลงไว้ตั้งแต่แรกว่า session ใหม่เดินเข้ามา
  session จบมุดหายไป แต่ยังไม่มีท่ารองรับ ทั้งคู่ใช้ `phase` เป็นความคืบหน้า 0→1 ไม่ใช่ลูปวน
- **เพิ่มท่า `conducting` (prop `crew`)** — session ที่กระจายงานให้ subagent มองไม่ออกเลย
  จากจอ: tool call ของ subagent ยิง hook ด้วย `session_id` เดียวกัน สล็อตพ่อจึงกระพริบ
  เป็น reading/writing ของงานที่ **ไม่ใช่ของมัน** ตอนนี้ `subagents > 0` ชนะ tool ทั้งหมด
  (แต่แพ้ `error` กับ `waiting` — พังและต้องการมือคน สำคัญกว่ากำลังยุ่ง)
  prop เป็นมาสคอตจิ๋วสองตัว ไม่ใช่ไม้กำกับวง: ที่ 3 px/unit ไม้เอียงบางๆ เละเป็นขั้นบันได
  และสารที่ต้องสื่อคือ "มีหลายตัวทำงานอยู่" ไม่ใช่อุปมาเรื่องวาทยกร
  `priority = 22` สูงกว่า tool (20) เพราะ session ที่คุม background subagent เงียบสนิท
  ไม่มี hook ยิงเป็นนาทีๆ ถ้าให้เท่ากันมันจะแพ้ tie-break ที่ `lastActivity` แล้วหลุดจอ
  ตัวนับต้องรีเซ็ตที่ `Stop`/`SessionStart`/`SessionEnd`/`UserPromptSubmit` — `SubagentStop`
  ที่หายไปตอน daemon ไม่ได้รัน จะทำให้ท่าค้างตลอดกาลถ้าไม่มีจุดล้าง
- **เพิ่มท่า `beacon` (prop `beacon`)** — เดิม `LSP` ถูกเหมาเป็น `reading` และ `mcp__*`
  เป็น `searching` ทั้งคู่ตีความผิด: ไม่ได้อ่านดิสก์และไม่ได้ค้นเว็บ แต่กำลัง**รอบริการอื่นตอบ**
  ซึ่งเป็นช่วงที่ค้างนานที่สุดและผู้ใช้ควรแยกออกว่ากำลังรอใครอยู่
  prop เป็นเสาอากาศ**บนหัว** ไม่ใช่เสาที่ถือไว้ข้างตัว — สัญญาณต้องออกจากตัวมาสคอตเอง
  ไม่ใช่จากอุปกรณ์ที่ตั้งอยู่ข้างๆ คลื่นเป็นส่วนโค้งสามชั้นที่แผ่ออก**ด้านข้าง**สองข้างของเสา
  ไม่ใช่ซ้อนขึ้นบน: เหนือหัวมีที่แค่ 5.6 unit แต่แนวนอนมีเหลือเฟือ ชั้นที่ไล่ออกข้างจึงเดินได้
  ไกลกว่าและอ่านเป็นคลื่นวิ่งออกจริง (วงกลมที่ขยายอย่างภาพอ้างอิงเล็กจนอ่านเป็นห่วงคาหัว)
  ยิ่งไกลสันยิ่งสูง จึงอ่านเป็นวงที่กว้างขึ้น ไม่ใช่ขีดสามขีดที่ยาวเท่ากัน
  ชั้นโผล่สะสมทีละชั้น 1 → 2 → 3 แล้ววนใหม่ = สัญญาณที่แผ่ออกไกลขึ้นเรื่อยๆ ชั้นที่ยังไม่ถึงคิว
  ไม่วาดเลย (ไม่มีโครงเทาค้าง) และใช้สีเหลืองสีเดียวทั้งชุด — หลายสีอ่านเป็นของหลายชิ้น
  ไม่ใช่คลื่นชุดเดียว
  ทุกชิ้นตั้งฉากเพราะเส้นทแยงบางๆ ที่ 3 px/unit เละเป็นขั้นบันได (บทเรียนเดียวกับ `crew`)
  เสาเป็นสีขาวเหมือนเส้นขอบตัว จึงอ่านเป็นชิ้นส่วนของมาสคอตเอง ไม่ใช่ของที่พิงอยู่
  ไฟยอดเสาเป็นวงกลมแดงคงที่ ไม่กะพริบ — จังหวะทั้งหมดอยู่ที่คลื่นแล้ว ถ้าไฟกะพริบด้วยจะแย่งกันเต้น
  ตาเป็นขนาดปกติ (ตาโตอ่านเป็นตกใจ ซึ่งเป็นสารของ `alert`) และ**ยกมือค้างสองข้าง**
  แบบเพ่งพลัง: แขนเป็นสองท่อนลดหลั่นออกนอกตัว (`arm_up` + `arm_out`) เพราะ nub ก้อนเดียว
  ที่เลื่อนขึ้นเฉยๆ อ่านเป็น "ไหล่สูงขึ้น" ไม่ใช่ "ยกมือ" และยกค้างนิ่ง ไม่โยก เพราะการโยก
  อ่านเป็นโบกมือ
  `priority = 20` เท่ากลุ่ม tool — เป็นงานที่ Claude ทำเองอยู่ ไม่ได้รอมือคนเหมือน `waiting`
- **ฟ้ากลางวันเป็นน้ำเงินกลาง `#3A5A72` ไม่ใช่ฟ้าอ่อนจริง** — มาสคอตสีดิน `#D97757`
  บนฟ้าอ่อน `#7FB4D9` ได้ contrast แค่ 1.5:1 ซึ่งบังคับให้เอาเส้นขอบขาวกลับมา
  (เพิ่งถอดออกไป) `#3A5A72` ได้ 2.2:1 — อ่านออกทันทีว่ากลางวัน โดยมาสคอตยังเป็นตัวเด่น
- **ดวงอาทิตย์ขึ้น/ตกที่ขอบจอพอดี (`arc_pad = 0`)** — ลองให้โผล่พ้นขอบจอข้างละ 24px
  แล้วดวงหายไปนอกจอตลอด 05:00–05:55 และ 18:05–19:00 ซึ่งกินเวลาเกือบครึ่งของ dawn/dusk
  ทั้งที่นั่นคือช่วงเดียวที่ต้องเห็นดวงติดขอบฟ้า
- **ฟ้ากลางวันต้องข้ามความสว่างของมาสคอตไปให้พ้น ไม่ใช่หยุดกลางทาง** — มาสคอตอยู่ที่
  luminance 0.27 พอดี ฟ้าที่สว่างใกล้ค่านั้น (`#7FB4D9` = 0.43) ได้ contrast แค่ 1.5:1
  ซึ่งแย่กว่าฟ้าเข้ม กราฟ contrast มีก้นบึ้งตรงนั้น `#B8DCF5` (0.67) ได้ 2.2:1 เท่ากับ
  ฟ้าน้ำเงินเข้มที่เคยลอง แต่เป็นกลางวันจริง — และทำให้เมฆต้องกลับด้านเป็นสีขาว
- **dawn กับ dusk อยู่คนละวงศ์สี** — เดิมเป็นม่วงทั้งคู่ต่างกันแค่ความสว่าง แล้วแยกไม่ออก
  ทั้งสองช่วงยาวช่วงละ 2 ชม. และไม่มีวันปรากฏพร้อมกัน ตัวแยกจึงต้องเป็นสีล้วนๆ
  ไม่มีบริบทอื่นช่วย: เช้า = น้ำเงินคราม (ฟ้ายังเย็น) · เย็น = แดงอมม่วง (ไอร้อนที่เหลือ)
  เมฆแยกสีตามด้วย ไม่ใช้สีร่วม ไม่งั้นมันดึงสองช่วงกลับมาเหมือนกันอีก
- **เงาใต้เท้า แยกสีตามช่วง** — ท่า `celebrate` มี `bob = 1.25 unit` = ยกเท้าพ้นพื้น 5px
  ตอนไม่มีพื้นดินมองไม่เห็น พอมีพื้นแล้วอ่านเป็นลอย เงาปักหมุดว่าพื้นอยู่ตรงไหน
  ขนาดคงที่ ไม่ยุบตามความสูง — เงาที่ยุบตามกลายเป็นสิ่งที่ต้องมอง แทนที่จะเป็นสิ่งที่
  ทำให้มองตัวละครถูก · เกาะกึ่งกลาง *ลำตัว* ไม่ใช่กึ่งกลาง slot เพราะ `build_centered`
  จัดกึ่งกลางกรอบที่รวม prop ด้วย · สีดำสีเดียวใช้ไม่ได้: ค่าที่เข้มพอสำหรับพื้นกลางคืน
  กลายเป็นรูดำบนพื้นกลางวัน
- **สีเขียวไปอยู่ที่กอหญ้า ไม่ใช่ผืนพื้น** — พื้นดินกินถึงก้นจอ ซึ่งเป็นที่ที่แผงโควตา
  วางตัวเลข % สีเขียวและนาฬิกาใหญ่วางตัวอักษรทับอยู่ ถ้าพื้นเป็นเขียวหญ้าจริง ตัวเลขจะจม
  พื้นทุกช่วงจึงเข้ม (luminance ~0.04) แล้วยกความเขียวไปไว้ที่กอหญ้าเล็กๆ แถวเดียว
  บนเส้นขอบฟ้า ซึ่งไม่มีตัวอักษรทับ · กอสูงไม่เท่ากัน (วน 3..6 px) เพราะกอสูงเท่ากันหมด
  อ่านเป็นรั้ว ไม่ใช่หญ้า · ก้านหนา 2px เว้นช่อง 1px: ก้าน 1px หายไปเลยบนแผงจริง
  ทั้งที่บน preview ที่ขยาย 3 เท่ามันเห็นชัด — และช่องว่างคือสิ่งที่ทำให้ยังอ่านเป็นกอ
  ไม่ใช่แถบทึบ
- **แถบมาสคอตนั่งต่ำลงจนเส้นขอบฟ้าอยู่กลางจอพอดี (93 -> 120)** — ที่ว่างระหว่างป้ายชื่อ
  กับแผงโควตาเดิมกว้างเปล่าประโยชน์ ส่วนฟ้าสูงแค่ 71px ทำให้ดวงอาทิตย์เดินโค้งเตี้ยติดหัว
  มาสคอต ตอนนี้ฟ้า 98px และเหนือหัวมาสคอตโล่ง 31px คือทางที่ดวงเดินโดยไม่โดนบัง
  (`arc_peak` 58 -> 82) · พิกัด y ของดาวและเมฆกระจายใหม่ทั้งชุดตามความสูงที่เปลี่ยน
- **การ์ดลดจาก 3 ใบเหลือ 2 ใบ + "+N more"** — ใบที่ 3 ดันแผงล่างขึ้นมาชนป้ายชื่อโปรเจกต์
  แต่การตัดใบทิ้งเงียบๆ ทำให้ "ไม่มีอะไรค้างแล้ว" กับ "ยังค้างอีกสองเรื่องแต่จอไม่พอ"
  หน้าตาเหมือนกัน จำนวนที่เกินจึงส่งมาบนสายเป็นคีย์ `m` แยกต่างหาก และนับรวมใบที่
  `Snapshot.encoded` ตัดทิ้งเพราะ MTU ล้นด้วย — เหตุผลที่การ์ดหายไปไม่สำคัญกับคนอ่านจอ
- **เส้นคั่น slot ถูกถอดออก** — เส้นตั้งเต็มความสูงพาดขึ้นไปกลางท้องฟ้าแล้วอ่านเป็นเสา
  ไม่ใช่ตัวคั่น และตัดฉากผืนเดียวออกเป็นสามชิ้น ลองย้ายลงมาไว้บนพื้นดินอย่างเดียวแล้ว
  ยังตัดพื้นอยู่ดี หน้าที่เดิมของมัน (กันไม่ให้ prop ของตัวหนึ่งอ่านเป็นของตัวข้างๆ)
  ตอนนี้เงาใต้เท้าทำแทน — มันบอกว่าแต่ละตัวยืนอยู่ตรงไหน ซึ่งตรงประเด็นกว่าเส้นแบ่งพื้นที่

## แผงโควตา (Session / Weekly utilization)

**ที่มาของตัวเลข** — Claude Code ป้อน JSON เข้า stdin ของคำสั่ง `statusLine` ทุกครั้งที่
render และใน JSON นั้นมี `rate_limits.five_hour` / `.seven_day` (`used_percentage` 0–100
กับ `resets_at` เป็น epoch) นี่คือแหล่งตั้งต้นและเป็นแหล่งเดียวที่ทำงานได้เอง —
**ไม่มี sessionKey ไม่ยิงเน็ต ไม่แตะ Keychain** และไม่ต้องมี timer เพราะ Claude Code
เป็นตัวเรียกให้เอง

ข้อจำกัดที่เอกสารระบุ: `rate_limits` มีเฉพาะบัญชี Pro/Max และเฉพาะหลัง API response แรก
ของ session · แต่ละหน้าต่างหายไปอิสระต่อกันได้

**ทางเดินของข้อมูล**
```
Claude Code --stdin--> ~/.tamaclaude/statusline.sh --.
                                   |                  \
                          (ส่งต่อให้ statusline เดิม)   >--> ~/.claude/.statusline-usage-cache
                                                      /                  |
claude.ai /api/.../usage --payload--> UsageWriter ---'        daemon อ่าน --> "u" --> บอร์ด
```

- **ยึดช่อง `statusLine.command` แต่ไม่เปลี่ยนสิ่งที่ผู้ใช้เห็น** — สคริปต์ของเราเขียน cache
  แล้วส่ง JSON ก้อนเดิมต่อให้คำสั่งเดิม แล้วพิมพ์ผลของมันออกไป การยึดช่องนี้คือการควบคุม
  *ท่อข้อมูล* ไม่ใช่การเปลี่ยน *หน้าตา* ทุกทางในสคริปต์จบด้วย `exit 0` เพราะ statusline
  ที่พังเพราะเราคือราคาที่แพงกว่าฟีเจอร์นี้ทั้งอัน
- **เขียนไฟล์เดียวกับ Claude Usage.app** (`~/.claude/.statusline-usage-cache`) คีย์ชุดเดิม
  เวลาเป็น ISO8601 เหมือนเดิม — สคริปต์ statusline ที่ผู้ใช้จูนไว้จึงอ่านต่อได้โดยไม่ต้องแก้
  ทั้งสองฝ่ายเขียนความหมายเดียวกัน ใครเขียนทีหลังก็ถูก และเขียนแบบ temp + rename
  จึงไม่มีใครอ่านเจอไฟล์ครึ่งๆ
- **ทางเข้าที่สอง ไม่ใช่ตัวเขียน cache ตัวที่สอง** — payload ของ `/api/organizations/<org>/usage`
  (รูปแบบดูที่ `docs/claude-usage-api.md`) เข้าทาง `UsageWriter.ingestAPI` ซึ่งอยู่ใน enum
  เดียวกับทางเข้าของ statusline และจบที่ `merge`/`write` ชุดเดียวกัน เหตุผลคือกฎ
  "ภายในหน้าต่างเดียวกัน เปอร์เซ็นต์เพิ่มอย่างเดียว" ต้องมีเจ้าของคนเดียว ถ้าแยกไปเขียน cache
  จากอีกที่ กฎจะมีสองสำเนาแล้ววันหนึ่งจะไม่ตรงกัน กับดักสองอันที่เทสต์ปักไว้: `utilization`
  เป็น float ส่วน `percent` เป็น int จึง**ปัด**ทั้งคู่ ไม่ตัดทิ้ง (16.6 ไม่งั้นได้ 16 ทางหนึ่ง
  17 อีกทางหนึ่ง) · และเวลาต้องผ่าน formatter ตัวเดิมก่อนเขียนเสมอ ไม่ส่ง ISO ดิบลงไฟล์
  เพราะ `…:00Z` กับ `…:00.482Z` คือหน้าต่างเดียวกันแต่คนละสตริง แล้ว merge จะนึกว่า
  หน้าต่างหมุนและปล่อยให้เปอร์เซ็นต์ถอยหลัง — เปอร์เซ็นต์ที่ไม่มี `resets_at` ที่อ่านออก
  จึงถูกทิ้งทั้งบาน ตัวเลขที่ไม่รู้ว่าอยู่หน้าต่างไหนเทียบกับกฎไม่ได้ · `weekly_scoped`
  เป็น limit ราย model ไม่ใช่ของทั้งบัญชี จึงไม่เคยถูกอ่านเป็น weekly
- **ตัวยิงเป็นโปรเซสอายุสั้น ไม่ใช่ daemon** — `tamaclaude --usage-poll` อ่าน key → หา org
  → ยิง → ส่งไบต์เข้า `ingestAPI` → พิมพ์บรรทัดสรุป → `exit` ตัวจับเวลาอยู่ฝั่ง menu bar
  โปรเซสที่ตายเองไม่ต้องมี supervision ไม่มี backoff ไม่มี crash loop ไม่มีลูกกำพร้า
  และ credential อยู่ใน RAM แค่ช่วงยิง · demo ที่ท่อ statusline ทำไม่ได้เลย: ปิด Claude Code
  ให้หมด รันคำสั่งมือ แล้วตัวเลขบนบอร์ดยังขยับ
- **`sessionKey` คือ credential เต็มบัญชี ไม่ใช่ key จำกัดสิทธิ์** — อยู่ในไฟล์
  `~/.tamaclaude/session-key` mode 600 เท่านั้น ไฟล์ที่ group หรือ other อ่านได้ถูกปฏิเสธ
  พร้อมบอกวิธีแก้ · ไม่เคยผ่าน argv (ทุกคนบนเครื่องเห็นด้วย `ps`) และไม่เคยผ่าน env —
  โปรเซสอ่านไฟล์เอง · **อ่านใหม่ทุกรอบ ไม่ cache ในหน่วยความจำ** เพราะ key หมดอายุได้
  และวิธีแก้ต้องเป็นแค่การ paste ทับไฟล์ ไม่ใช่การไล่หาโปรเซสมารีสตาร์ท · ห้าม log
  request หรือ header ทุกกรณี เพราะ cookie อยู่ในนั้น ข้อความ error จึงสร้างจาก status code
  ล้วนๆ · org id ถูก validate (ปฏิเสธ `/` และ `..`) ก่อนต่อเข้า URL เสมอ แม้จะมาจาก
  response ของเราเอง — ปลายทางเป็นของคนอื่น ทุกค่าที่กลับมาจึงเป็นค่าภายนอก
- **exit code แยกชนิดความล้มเหลว** — `2` key ถูกปฏิเสธ (401/403 ต้องไปแปะอันใหม่) ·
  `3` ไฟล์ key ใช้ไม่ได้ · `1` อย่างอื่น (5xx, เน็ตหลุด, timeout) ผู้เรียกจึงแยก
  "ผู้ใช้ต้องลงมือ" ออกจาก "เดี๋ยวรอบหน้าก็หาย" ได้โดยไม่ต้องอ่านข้อความ · ล้มเหลวแบบไหน
  ก็ **ไม่ลบและไม่แตะ cache เดิม** ค่าที่ค้างอยู่ยังจริงกว่าการไม่มีค่าเลย
- **ตัวจับเวลาอยู่ที่ menu bar ตัวเดียว** — `UsagePoller` ถูกป้อน `tick(now:)` จากนาฬิกา
  วินาทีละครั้งที่แอปมีอยู่แล้ว ไม่มี timer ตัวที่สอง และไม่มีตัวจับเวลาในโปรเซสลูกให้ต้อง
  ซิงก์กัน ผลพลอยได้คือตรรกะทั้งอันเป็นฟังก์ชันของเวลาที่ส่งเข้ามา เทสต์ครบทุกกิ่งได้โดย
  ไม่ต้องรอของจริงสักวินาที · ลูกทีละตัวเท่านั้น และลูกที่ไม่จบใน **30 วินาที** ถูกฆ่า
  (TERM แล้ว KILL) ไม่งั้นลูกที่ค้างจะกองกันทุกนาที · ปิดแอป = ฆ่าลูกก่อนตาย ไม่ทิ้งกำพร้า ·
  ลูกที่ถูกฆ่าไปแล้วยังพูดทีหลังได้ จึงมีเลขรุ่นกำกับ เสียงจากอดีตทับสถานะปัจจุบันไม่ได้
- **รอบให้เลือกสามค่า: `Off` / `60s` (ตั้งต้น) / `5 min` ไม่มี 30 วินาที** — 60 วินาที
  คือจังหวะที่ Claude Usage Tracker ใช้อยู่จริง และเร็วเท่าที่ค่าจะขยับอยู่แล้ว
  (หน้าต่าง 5 ชม. ขยับราวหนึ่งเปอร์เซ็นต์ต่อสามนาที) ตัวเลือกที่ถี่กว่านั้นคือการยิงเพิ่ม
  เพื่อได้ค่าเดิม · `Off` เป็นค่าที่ผู้ใช้เลือกได้จริง จึงต้องแยกจาก "ยังไม่เคยเลือก" ให้ออก
  ตอนอ่านค่าที่จำไว้ ไม่งั้นแอปที่เพิ่งติดตั้งจะกลายเป็น `Off` เงียบๆ
- **หยุดยิงเมื่อ key พัง ไม่หยุดเมื่อเน็ตพัง** — exit `2` (401/403) เป็นอันเดียวที่ *ล็อก*
  การยิงไว้ เพราะยิงต่อไปก็ได้ 401 เหมือนเดิมทุกนาที และไม่มีอะไรนอกจากคนที่เปลี่ยนมันได้
  ตั้ง key ใหม่แล้วปลดล็อกและยิงทันทีเอง · exit `3` (ไฟล์ key ใช้ไม่ได้) เป็นแค่ป้ายบอกอาการ
  ไม่ใช่ล็อก ตัวที่กันไม่ให้ยิงคือการอ่านสภาพไฟล์จริงทุกรอบ ผู้ใช้ที่ `chmod 600` เอง
  ข้างนอกจึงกลับมายิงได้โดยไม่ต้องเปิดปิดแอป — ล็อกที่ปลดได้ทางเดียวคือกับดัก ·
  exit `1` (5xx, เน็ตหลุด)
  ยิงต่อตามรอบ เพราะรอบหน้าก็หายเอง · เครื่องตื่นจาก sleep ยิงทันทีหนึ่งรอบ ไม่รอครบรอบ
  ถัดไป — ตื่นมาเจอตัวเลขเมื่อสองชั่วโมงที่แล้วคือหน้าจอที่โกหก
- **สองบรรทัดที่พูดคนละเรื่อง** — "key หมดอายุ" (ท่อพัง ต้องลงมือ กดที่บรรทัดเพื่อใส่ key
  ใหม่ได้เลย) กับ "ค่านี้อายุเท่าไร" (ค่าที่เห็นเก่าแค่ไหน) ยุบเป็นบรรทัดเดียวเมื่อไร ผู้ใช้
  จะแยกไม่ออกว่า "เก่า" แปลว่าต้องทำอะไรหรือไม่ต้องทำอะไร — ค่าเก่ายังถูกได้เสมอในโดเมนนี้
- **แอปเป็นคนเขียนไฟล์ key ไม่ใช่ผู้ใช้** — ช่องกรอกแบบปิดบังตัวอักษร แล้วเขียนไฟล์
  mode 600 ตั้งแต่วินาทีแรกที่ไฟล์มีตัวตน (สร้าง temp ด้วย 600 แล้ว rename ไม่ใช่เขียนก่อน
  แล้ว `chmod` ทีหลัง — ช่วงระหว่างสองคำสั่งนั้นคือช่วงที่ credential เปิดให้คนอื่นอ่าน)
  ต้องลบไฟล์เดิมก่อน rename ด้วย เพราะ `replaceItemAt` คัดลอกสิทธิ์ของไฟล์เดิมกลับมา
  ไฟล์ 644 ที่ผู้ใช้เคยสร้างเองจะยังเป็น 644 ต่อไปทั้งที่เพิ่งเขียนใหม่
- **สถานะจากลูกเดินทางมาทาง stdout ทางเดียว ไม่มีไฟล์สถานะตัวที่สอง** — รวมถึงตอนล้มเหลว
  (ข้อความออก stdout ไม่ใช่ stderr) และรวมถึงรายการ org: บรรทัด `org <id> <ชื่อ>` นำหน้า
  บรรทัดสรุป ลูกตายทุกรอบ อะไรที่มันรู้จึงต้องพูดตอนยังมีชีวิตอยู่ ไฟล์สถานะจะค้างเป็นค่าเก่า
  ทันทีที่ลูกตายกลางคัน · รายการ org ไปกับความล้มเหลวด้วย (`Failure.orgs`) ไม่งั้นการพินไว้ที่
  org ที่หายไปจะล็อกตัวเอง: ยิงพลาดทุกรอบ และไม่เคยได้รายการมาให้เปลี่ยนใจ · id จาก stdout
  ยัง `validated` ซ้ำ เพราะทางเดินของมันจบที่ URL เหมือนกัน · org เดียว = เมนูซ่อนทั้งอัน
  (ไม่มีอะไรให้ตัดสินใจ) หลาย org = ใช้ตัวแรกไปก่อน พร้อมตัวเลือกสลับ ติ๊กอ่านจากตัวที่ยิงจริง
  ไม่ใช่ค่าที่จำไว้ — ตัวที่จำไว้แล้วหายไปจากบัญชีจะทำให้ไม่มีติ๊กสักอันทั้งที่กำลังยิงอยู่ตัวหนึ่ง
- **พินคือพินในลูก การถอยเป็นการตัดสินใจของพ่อ** — `TAMACLAUDE_ORG_ID` ที่ส่งเข้าลูกถูก
  ใช้ตามนั้นเสมอ ไม่ถูกแทนเงียบๆ ด้วยตัวแรก (ผู้ใช้ที่ตั้ง env เองจะได้ตัวเลขของ org อื่น
  โดยไม่มีอะไรบอก) ส่วนการถอยไปตัวแรกเมื่อตัวที่เลือกไว้หายจากบัญชีอยู่ที่ menu bar
  ซึ่งเป็นฝ่ายที่เห็นรายการทั้งอัน
- **ท่อ statusline กับท่อ poll เป็นสวิตช์คนละตัว** — ตั้ง key สำเร็จแล้วไม่ auto-uninstall
  statusline ทั้งสองทางเขียน cache เดียวกันผ่านกฎชุดเดียวกันอยู่แล้ว การถอนอันหนึ่งให้
  อัตโนมัติคือการแตะ `settings.json` ของผู้ใช้โดยที่เขาไม่ได้สั่ง
- **ส่ง "วินาทีที่เหลือ" ไม่ใช่เวลารีเซ็ต** — `"u":[[35,10800],[48,111600]]` บอร์ดนับถอยลงเอง
  ผลคือ countdown ยังเดินถูกตอน BLE หลุด และ daemon ไม่ต้องยิงใหม่ทุกนาที
  ค่าที่ส่งถูกปัดลงเป็นนาที ไม่งั้น snapshot ต่างกันทุกวินาทีแล้วบอร์ดโดนยิงวินาทีละครั้ง
- **ขีด pace มาฟรี** — ความยาวหน้าต่างเป็นค่าคงที่ (18000 / 604800 วิ) บอร์ดจึงหา
  `elapsed = window - remaining` เองได้ ไม่ต้องส่งอะไรเพิ่ม ขีดอยู่ขวาของเนื้อแถบ =
  ใช้ช้ากว่าเวลา · อยู่ซ้าย = ใช้เร็วเกินไป — 35% ไม่ได้แปลว่าดีหรือแย่จนกว่าจะรู้ว่า
  ผ่านไปกี่เปอร์เซ็นต์ของหน้าต่างแล้ว
- **ไม่มี TTL** — เปอร์เซ็นต์ขยับได้ก็ต่อเมื่อผู้ใช้เรียก Claude ค่าที่เก่าจึง *คือ* ค่าที่ถูก
  สิ่งเดียวที่ทำให้หมดอายุคือ `resets_at` ที่ผ่านไปแล้ว ซึ่งเป็นเส้นตายจริงในโดเมนนี้
- **ไม่รู้ ≠ ศูนย์** (ADR-0001 ของ `esp32-claude-quota`) — ศูนย์เป็นค่าจริง `-1` คือไม่รู้
  ไม่เคยมีข้อมูลเลย → ซ่อนแผงทั้งอันแล้วกลับไปเป็นนาฬิกา (โครงเปล่าดูเหมือนอุปกรณ์พัง)
  มีทีละตัว → แถวที่ขาดเป็น `--` · countdown ถึง 0 → เปอร์เซ็นต์กลายเป็น `--` ทันที
  เพราะหน้าต่างหมุนไปแล้วและเราไม่มีทางรู้ค่าใหม่
- **ห้ามเขียนค่าที่เก่ากว่าทับ** — `rate_limits` ใน stdin คือค่าจาก API response ล่าสุด
  *ของ session นั้น* ซึ่งค้างได้ระหว่างไม่มีการเรียก API ส่วนแหล่งอื่นที่เขียนไฟล์เดียวกัน
  (Claude Usage.app ที่ยิง API เอง, อีก session ที่กำลังคุยกับ Claude) อาจรู้ค่าที่ใหม่กว่า
  invariant ที่ใช้ตัดสิน: **ภายในหน้าต่างเดียวกัน (`resets_at` ตรงกัน) เปอร์เซ็นต์เพิ่ม
  อย่างเดียว** ค่าที่ต่ำกว่าจึงเก่ากว่าเสมอ พอหน้าต่างหมุน `resets_at` เปลี่ยน
  ค่าที่ลดลงกลายเป็นค่าที่ถูก · คีย์ของเจ้าของร่วม (`PROFILE_NAME`, `COST_*`) ต้องรอดไป
- **`refreshInterval: 10`** — เอกสารระบุว่า statusline เป็น event-driven ล้วนและ trigger
  จะเงียบตอน session ว่าง ซึ่งเป็นสภาพที่จอนี้เจอเกือบตลอดเวลา
- **ติดตั้งซ้ำต้องไม่ลืมคำสั่งเดิม** — ตอนอัปเกรด `statusLine.command` ชี้มาที่เราอยู่แล้ว
  คำสั่งเดิมของผู้ใช้เก็บอยู่ในตัวสคริปต์ (`PREV=`) ต้องกู้จากตรงนั้น
  ไม่งั้นการติดตั้งซ้ำ = ลบ statusline ของเขาทิ้งเงียบๆ
- **มีการ์ด = โควตาย่อขึ้นแถบบน** — แถบสั้น 34x6px + `52%` เฉพาะหน้าต่าง 5 ชม.
  **ไม่มีป้าย `5h`** เพราะบนแถบมีค่าเดียว ไม่ต้องบอกว่าตัวไหน พื้นที่นั้นไปทำแถบคุ้มกว่า —
  เหลือบแล้วรู้ทันทีโดยไม่ต้องอ่านตัวเลข (แถบ 6px บนแถบ 22px อ่านออก ต่างจากแถบบาง
  ที่มองไม่ออก) countdown ใส่ไม่ลง ตัดทิ้ง เพราะตอนมีการ์ดสิ่งที่ต้องทำคือจัดการ
  notification ไม่ใช่วางแผนโควตา · ทั้งชุดหลบตำแหน่งเมื่อ `+N` โผล่
  เลือก 5 ชม. เพราะเป็นตัวที่ขยับเร็วพอจะเปลี่ยนการตัดสินใจภายในวันเดียว
  ส่วน weekly รอดูตอนการ์ดหายไปได้ — พอการ์ดหมด แผงเต็มกลับมาแทน
- **สีบอกระดับ 3 ขั้น ไม่มีการเตือน** — <60 เขียว · 60–85 เหลือง · >85 แดง
  ไม่มี card ไม่มี LED จากโควตา เพราะ card จะไปบังแผงโควตาเอง (ย้อนแย้ง) และ LED
  ที่แปลว่า "Claude ต้องการคุณ" อยู่แล้วจะมีสองความหมายจนแยกไม่ออก
- **ไม่มี session = มาสคอตเดินเล่น** — แถบ slot ที่ว่างทั้งแถบอ่านได้ว่าอุปกรณ์ค้าง
  แทนที่ด้วยมาสคอตตัวเดียวเดินจากนอกจอซ้ายไปนอกจอขวา (34 px/วิ) แล้ววนใหม่
  หยุดทำท่าหนึ่งครั้งกลางทาง 2.5 วิ วนสี่ท่า (celebrate/thinking/searching/waiting)
  ตำแหน่งหยุดวนสามค่า (0.34/0.5/0.66 ของเส้นทาง) — จำนวนไม่เท่ากันจึงไม่จับคู่ซ้ำ
  ตำแหน่งคำนวณจาก `cycle + phase` ที่เดินอยู่แล้ว ไม่ต้องมี state เพิ่ม
  และไม่ต้องพึ่ง BLE — จอที่ไม่มีสายก็ยังมีชีวิต
- **แซง pace = แดงก่อนถึงเกณฑ์** — แถบ (และตัวเลข) เป็นแดงทันทีที่ `pct > elapsed/window`
  ไม่ต้องรอ 85% เพราะ "60% ตอนเหลือเวลาอีกครึ่ง" เป็นปัญหาคนละแบบกับ "60% ตอนหมดเวลาพอดี"
  ขีด pace ยังอยู่ — สีบอก *ว่ามีปัญหา* ขีดบอก *ว่าห่างแค่ไหน*
- **ป้าย pill สีคงที่ต่อหน้าต่าง** — Current ส้ม (clay) · Weekly เขียว (good) ตัวอักษรสีหมึก
  สีไม่ตามระดับการใช้ ถ้าทั้งแถวเป็นสีเดียวตอนวิกฤต สีจะหยุดเป็นสัญญาณ
  แถบเป็นทรง pill (radius = ครึ่งความสูง) รางใช้ `gray_dark` ไม่ใช่ `bg_slot` ซึ่งกลืนพื้นจอ

## ต้องยืนยันก่อนเขียน firmware

1. ~~บอร์ดอยู่ในมือแล้วหรือยัง~~ — ยืนยันแล้ว: ESP32-D0WD-V3 rev 3.1, 4MB flash,
   ไม่มี PSRAM, พอร์ตมักเป็น `/dev/cu.usbserial-*` (ดูด้วย `ls /dev/cu.*`)
   ส่วน MAC ของบอร์ดอ่านได้จาก log ตอนบูต — 2 ไบต์ท้ายของมันคือชื่อที่บอร์ดใช้โฆษณาบน BLE
2. ~~ชิปจอเป็นตัวไหน~~ — วัดจากบอร์ดจริงแล้ว ดู "ผลตรวจจอ" ด้านล่าง
3. **ฟอนต์บน firmware ต้องครอบคลุมอักขระที่ daemon ส่งมา** — เจอตอนทำ preview:
   ข้อความที่มี `—` (em dash) กลายเป็นกล่องสี่เหลี่ยม
   daemon ต้องกรองข้อความให้เหลือเฉพาะชุดอักขระที่ฟอนต์บนบอร์ดมีจริง ก่อนส่งบน BLE

## ผลตรวจจอ (วัดจากบอร์ดจริงด้วย `firmware/probe/`)

```
MADCTL          0x28   = MV | BGR   -> แนวนอน 320x240 origin มุมบนซ้าย
ลำดับสี         BGR    ไม่ใช่ RGB
Inversion       ปิด    (INVOFF)
COLMOD          0x55   16 bit/pixel
```

ขา SPI ตามเอกสารชุมชน **ยืนยันแล้วว่าถูกทั้งหมด** — จอตอบกลับผ่าน MISO จริง:

```
TFT (SPI2):   MOSI 13  MISO 12  SCLK 14  CS 15  DC 2  RST -1  BL 21   [ยืนยันแล้ว]
RGB LED:      R 4   G 16   B 17          (active low)                  [ยังไม่ได้ตรวจ]
Touch XPT2046 (SPI แยก):  CLK 25  MOSI 32  MISO 39  CS 33  IRQ 36     [v2]
microSD:      CS 5  MOSI 23  MISO 19  SCK 18                          [ไม่ใช้]
Speaker:      GPIO 26 (DAC1)                                          [v2]
```

**เลขรุ่นชิปพิสูจน์ไม่ได้ และไม่จำเป็นต้องพิสูจน์** — จอตัวนี้ล็อกการอ่าน ID ไว้
`0x04`, `0xD3`, `0xDA/DB/DC` คืนศูนย์ทั้งหมด มีแต่ `0x0A` (power mode) ที่ตอบ `0x98`
ซึ่งเป็นค่าเดียวกันทั้ง ILI9341 และ ST7789

แต่พฤติกรรมชี้ชัดไปทาง **ILI9341**: inversion ต้องปิดถึงจะสีถูก ส่วนโมดูล ST7789
เกือบทั้งหมดต้องเปิด inversion สิ่งที่ firmware ใช้จริงคือค่าคอนฟิกด้านบน ไม่ใช่เลขรุ่น
ถ้าวันหนึ่ง init เต็มของ ILI9341 ทำให้ภาพเพี้ยน ค่อยกลับมาทบทวนข้อสรุปนี้

## ความเสี่ยงที่รู้ตัว

- ~~มาสคอตสื่ออารมณ์ได้จำกัด~~ — **ปิดแล้ว** ทำครบ 11 สถานะและแยกออกจากกันได้ทั้งหมด
  ดู `out/states.png` ตัวชี้ขาดคือแยก mood (ตา/ลำตัว) ออกจาก prop (ของที่ถือ) เป็นคนละมิติ
- ~~80x80px ต่อตัว props อาจเล็กจนดูไม่ออก~~ — **ปิดแล้ว** ที่ 3px ต่อ unit
  แว่นขยาย/ค้อน/แล็ปท็อป/ลูกโลก อ่านออกหมด
- **Python preview ≠ ของจริง** — ยังเปิดอยู่ พิสูจน์ได้แค่การออกแบบ ไม่ได้พิสูจน์ renderer ฝั่ง C
  บรรเทาด้วย `layout.toml` → `layout.h` แต่ยังไม่ได้ทดสอบบนบอร์ด
- **ฟอนต์ preview ≠ ฟอนต์บนบอร์ด** — ระยะห่างตัวอักษรและการตัดข้อความจะไม่ตรงเป๊ะ
  ตำแหน่งที่ข้อความจะขาดบนจอจริงจึงยังไม่แน่นอน

## ฝั่ง Mac (Swift)

```
host/Sources/TamaCore/Protocol.swift   HookEvent, VisualState, Snapshot + การบีบให้พอดี MTU
host/Sources/TamaCore/Text.swift       กรองอักขระให้เหลือเฉพาะที่ฟอนต์บนบอร์ดวาดได้ + ตัดความยาว
host/Sources/TamaCore/ToolMap.swift    tool -> สถานะภาพ (แก้ได้ที่ ~/.tamaclaude/tools.json)
host/Sources/TamaCore/SessionStore.swift  ตรรกะทั้งหมด: hook -> สถานะ -> snapshot
host/Sources/TamaCore/SocketServer.swift  Unix socket ระหว่าง hook กับ daemon
host/Sources/TamaCore/BLETransport.swift  CoreBluetooth central + auto-reconnect
host/Sources/TamaCore/Daemon.swift     ประกอบทุกอย่าง + timer 1 วิ
host/Sources/TamaCore/HookInstaller.swift  เขียน hook เข้า ~/.claude/settings.json
host/Sources/tamatest/                 ชุดเทสต์ (รันด้วย `swift run tamatest`)
```

คำสั่ง: `tamaclaude --hook` · `--daemon [--print] [--no-ble] [-v]` · `--install-hooks` · `--send <json>`

**GATT UUID** — firmware ต้องใช้ชุดเดียวกัน
```
service  7A9B0001-4C1E-4B6D-9E2A-1D5C3F0A0001
state    ...0002   daemon เขียน snapshot ลงตัวนี้ (write with response)
config   ...0003   ความสว่าง ฯลฯ
event    ...0004   บอร์ดแจ้งกลับ — สงวนไว้ v2
```

**snapshot** — คีย์สั้นเพราะต้องพอดี 500 ไบต์ (MTU 517 หัก header แล้วเผื่อ)
```json
{"c":"14:32","d":"Mon 27 Jul","o":0,
 "s":[{"p":"tamaclaude","s":"building"}],
 "n":[{"t":"tamaclaude","b":"allow Bash?","k":"alert"}]}
```
เกินโควตาเมื่อไร daemon จะหด `n` (body ก่อน แล้ว title แล้วตัดใบล่างทิ้ง) แต่ไม่เคยตัด `s`
— session คือสิ่งที่จอนี้มีไว้แสดง encode ด้วย `sortedKeys` เสมอ ไม่งั้นการเทียบว่า
"เปลี่ยนไหม" จะจริงทุกครั้งแล้วบอร์ดโดนยิงทุกวินาที

**สิ่งที่ตัดสินใจเพิ่มตอนเขียน daemon**

- **นอนชนะการทวงถาม** — session ที่ Stop แล้วเงียบเกิน 45 วิ ขึ้นท่า `waiting`
  แต่พอเงียบครบ 5 นาทีจะเปลี่ยนเป็นนอน ท่ายืนทวงตลอดกาลไม่ได้สื่ออะไรเพิ่ม
  ส่วนการเตือนยังอยู่ในรูปการ์ด ซึ่งเป็นคนละช่องทางกับท่าของมาสคอต
- **เกิน 3 ตัว: เลือกด้วยความสำคัญ แต่วาดตามลำดับเกิด** — ตัวที่ตกจอคือตัวที่เงียบสุด
  และเก่าสุด ลำดับซ้าย→ขวาของตัวที่เหลือไม่เคยสลับ
- **ทุกท่าอยู่บนจออย่างน้อย 5 วิ (`Timings.minPose`)** — Read/Edit จบใน ~100 มิลลิวินาที
  ถ้าเปลี่ยนท่าตามเหตุการณ์ทันที ท่านั้นจะสั้นกว่าหนึ่งเฟรมของบอร์ด (tick 1 วิ + ดีเลย์ BLE)
  ผู้ใช้จึงเห็นแต่ `thinking` ตลอด ท่าที่ถูกข้ามระหว่างหน่วงหายไปเลย ไม่เข้าคิว —
  คิวจะทำให้มาสคอตเล่าอดีตช้ากว่าความจริงเรื่อยๆ เมื่อเครื่องมือยิงรัว
  ข้อยกเว้น: ท่าที่ priority สูงกว่า (error/waiting) แทรกได้ทันที และ entering/leaving
  ไม่ถูกหน่วงเพราะมีนาฬิกาของตัวเองอยู่แล้ว
- **hook ต้องคืน 0 เสมอ** แม้ daemon ไม่ทำงาน — hook ที่พังจะไปทำให้ session ผู้ใช้พังตาม
  ซึ่งแย่กว่าจอไม่ขยับมาก
- **ไม่ใช้ `testTarget`** — เครื่องที่มีแค่ Command Line Tools จะ build ชุดเทสต์ผ่าน
  แต่ไม่รันมันเลยและยัง exit 0 ซึ่งอันตรายกว่าไม่มีเทสต์ จึงทำเป็น executable ธรรมดา

**การแจกจ่าย**

```
host/Scripts/make-app.sh              บิลด์ release -> host/dist/tamaclaude.app
host/Scripts/make-app.sh --install    ติดตั้งลง /Applications แล้วเปิดให้
tools/make_icon.py                    ไอคอนแอปจากมาสคอตตัวเดียวกับบนจอ -> AppIcon.icns
```

- **เลือกบอร์ดจากเมนูของแอป ไม่ใช่หน้า Bluetooth ของ macOS** — BLE peripheral ที่เป็น
  GATT ล้วน (ไม่ใช่ HID/audio) ไม่โผล่ในหน้าตั้งค่าให้จับคู่เลย ระบบเปิดทางให้เฉพาะแอป
  ที่สแกนเองผ่าน CoreBluetooth เมนูจึงมีรายการบอร์ดพร้อมติ๊กตัวที่เลือก จำข้ามการเปิดปิด
  ด้วย `UserDefaults` ค่าเริ่มต้น "Any board" ถูกต้องเมื่อมีจอเดียว
- **บอร์ดโฆษณาชื่อ `tamaclaude-<MAC 2 ไบต์ท้าย>`** — หลายบอร์ดในห้องเดียวกันต้องแยกออก
  จากกันได้ ชื่อซ้ำกันหมดแปลว่าผู้ใช้เลือกไม่ถูกว่าตัวไหนคือตัวไหน
- **daemon สแกนต่อแม้ต่อบอร์ดได้แล้ว** — ไม่งั้นรายการในเมนูจะมีแค่ตัวที่ต่ออยู่ตัวเดียว
  และสลับไปตัวอื่นไม่ได้จนกว่าจะถอดปลั๊ก
- **เมนูบาร์ *เป็น* daemon เอง** ไม่ใช่ตัวสั่ง daemon อีกตัว — สิทธิ์ Bluetooth ผูกกับ `.app`
  ที่ถูกปล่อยผ่าน LaunchServices แยกโปรเซสเมื่อไรได้ daemon ที่ต่อบอร์ดไม่ได้ทันที
- **`-v` และ `-psn_...` ต้องไม่ถูกอ่านเป็นโหมด** — LaunchServices แถม `-psn_0_12345`
  มาให้ตอนเปิดจาก Finder แอปที่ปฏิเสธอาร์กิวเมนต์ที่ไม่รู้จักจะตายทันทีที่ดับเบิลคลิก
- **ไอคอนแถบเมนูคือแถบโควตา 5 ชม. ไม่ใช่โลโก้** — รูปจอคอมพิวเตอร์นิ่งๆ กินพื้นที่ถาวร
  เพื่อบอกอย่างเดียวว่า "แอปยังรันอยู่" ซึ่งไม่มีใครต้องรู้ตลอดเวลา ส่วนสิ่งที่ต้องรู้จริง
  (เหลือโควตาเท่าไร) ต้องกดเมนูลงไปดู แทนที่ด้วยแถบวัด 35x11 + `42%` ตัวเลขความกว้างคงที่
  (ไม่งั้นไอคอนขยับซ้ายขวาทุกครั้งที่เปลี่ยนหลัก) เลือกหน้าต่าง 5 ชม. ตัวเดียวด้วยเหตุผล
  เดียวกับแถบย่อบนจอ — weekly รอดูตอนกดเปิดเมนูได้
- **แถบเมนูมีขีด pace ด้วย ไม่ใช่มีแต่สี** — สีตอบ "แซงหรือยัง" ซึ่งเป็นคำตอบแบบมี/ไม่มี
  ขีดตอบสิ่งที่สีตอบไม่ได้: แซงไป*เท่าไร* และอีกนานแค่ไหนกว่าเวลาจะไล่ทัน แถบที่โตขึ้น
  เป็น 34 px มีที่ให้ขีดยืนโดยไม่เบียดตัวเลข `MenuBadge` จึงเก็บ `pace` เป็นตำแหน่ง
  ไม่ใช่ `isAlarming` เป็น bool อีกต่อไป — สีคำนวณกลับจากตำแหน่งได้ แต่ตำแหน่ง
  คำนวณกลับจากสีไม่ได้ ขีดสูงเท่าแถบพอดี ตรงที่มันพาดผ่านเนื้อแถบเจาะเป็น*ร่องโปร่ง*
  ไม่ใช่วาดหมึกทับหมึกซึ่งมองไม่เห็น ส่วนตรงที่อยู่บนรางว่างยังวาดด้วยหมึก
  (บนจอใช้วิธีล้นขอบแทน เพราะที่นั่นวาดทับพื้นทึบ ไม่มี alpha ให้เจาะ)
  เส้นขอบจึงต้องวาดท้ายสุด ไม่งั้นร่องที่ทะลุขอบทำให้กรอบขาดเป็นสองท่อน
- **แถบมีเส้นขอบทึบ ไม่ใช่รางจางๆ อย่างเดียว** — พื้นหลังของแถบเมนูคือวอลเปเปอร์ของผู้ใช้
  ไม่ใช่สีทึบ รางที่จางพอจะไม่แย่งสายตากับตัวเลขก็จางพอจะหายไปกับภาพข้างหลัง เส้นขอบ 1 px
  คือสิ่งที่บอกว่าแถบเริ่มและจบตรงไหน ซึ่งเป็นข้อมูลที่ทำให้ "เต็มไปเท่าไร" อ่านได้จริง
  และเนื้อแถบเว้นจากขอบ 1.5 px ทั้งสี่ด้าน — ขอบที่ติดเนื้อกลายเป็นก้อนเดียวกัน
  แล้ว "เต็มแค่ไหน" ต้องเดาจากน้ำหนักสีแทนที่จะอ่านจากระยะ
- **มุมมน 2.5 ไม่ใช่ครึ่งความสูง** — pill อ่านเหมือนปุ่มหรือป้าย ส่วนนี่คือเครื่องวัด
  และปลายที่โค้งจนกลมทำให้ 3% แรกกับ 3% สุดท้ายกินพื้นที่ไม่เท่ากับ 3% ตรงกลาง
- **บนแถบเมนูมีสองสถานะ ไม่ใช่สามขั้น** — ปกติเป็น template ขาวดำ (ระบบกลับสีให้เอง
  ทั้งพื้นสว่าง/มืด) · ต้องเตือนเมื่อไรเลิกเป็น template แล้ววาดแดงตรงๆ เพราะสีที่ระบบ
  กลับได้ตามใจแปลว่า "แดง" ไม่ได้ ไม่มีเหลืองไม่มีเขียว: จอบนโต๊ะไล่สีสามขั้นได้เพราะ
  มีที่ให้ไล่ แต่ไอคอน 16 px ที่เหลือบครึ่งวินาทีมีคำถามเดียวคือ "ต้องช้าลงไหม"
- **แซง pace เป็นเกณฑ์เดียวบนแถบเมนู ไม่มีเกณฑ์เปอร์เซ็นต์ตายตัว** — จอบนโต๊ะมี
  `crit_pct` ด้วยเพราะมันไล่สีสามขั้นอยู่แล้ว 85 จึงเป็น*ขั้นสุดท้าย*ของการไล่
  แต่แถบเมนูมีสองสถานะ สีจึงต้องตอบคำถามเดียวคือ "ต้องช้าลงไหม" ซึ่ง 90%
  ตอนเหลือเวลาอีกนิดเดียวไม่ใช่ — โควตานั้นถูกใช้ตามแผนพอดี สีแดงที่ขึ้นทุกครั้ง
  ที่ใกล้หมดหน้าต่างคือสีที่ขึ้นตอนไม่มีอะไรให้ทำ แล้วมันจะหยุดเป็นสัญญาณ
  สูตร `pct > elapsed/window` ยังตรงกับ `usage_bar_color` ทั้งสองฝั่ง
  เอามาแค่ไม่เอาเกณฑ์เปอร์เซ็นต์ · ไม่รู้ countdown = เทียบ pace ไม่ได้ = ไม่เตือน
- **ตอนเมนูเปิด แบดจ์กลับไปเป็น template แม้กำลังเตือน** — ระบบถมพื้นปุ่มด้วยสีเน้น
  แล้ววาดภาพที่ไม่ใช่ template ทับตรงๆ แดงบนน้ำเงินอ่านไม่ออก และนั่นคือสถานะที่ผู้ใช้
  มองอยู่นานที่สุดตอนมันแดง (เขากดเพราะมันแดง) เสียสีแดงชั่วขณะที่เมนูกางอยู่แล้ว
  ดีกว่าเสียตัวเลขไปทั้งตัว
- **ไม่รู้โควตา → ไอคอนเดิม ไม่ใช่ `0%` ไม่ใช่แถบเปล่า** — `0%` ที่เดาเอาคือคำโกหก
  ที่ผู้ใช้เชื่อทันทีเพราะดูเหมือนค่าที่วัดมา ส่วนแถบเปล่าดูเหมือนแอปพัง
  (หลักเดียวกับ "ไม่เคยมีข้อมูล → ซ่อนแผงทั้งอัน" บนบอร์ด) countdown ถึงศูนย์นับเป็น
  "ไม่รู้" ด้วย — หน้าต่างหมุนไปแล้วและเราไม่มีทางรู้ค่าใหม่
- **เลิกหรี่ไอคอนตอนต่อบอร์ดไม่ติด** — พอไอคอนกลายเป็นตัวเลขโควตา การหรี่กลายเป็น
  การทำให้ข้อมูลที่ยังถูกต้องอยู่ (โควตาไม่ได้มาจากบอร์ด) ดูเหมือนใช้ไม่ได้
  สถานะบอร์ดอยู่ที่ท้าย popover
- **คลิกไอคอนได้ popover ไม่ใช่เมนู แต่เฟืองในนั้นเด้ง NSMenu ตัวเดิมทั้งอัน** — การ์ด
  โควตาที่มีแถบ ขีด pace และเวลารีเซ็ตวาดใน NSMenu ไม่ได้ ส่วนตัวเลือกบอร์ดพร้อมติ๊ก
  slider ความสว่าง สวิตช์ที่มีเครื่องหมายถูก และ Quit ได้มาฟรีจาก NSMenu และทำงานถูก
  อยู่แล้ว การเขียนใหม่เป็น NSView ล้วนคือการรื้อ UI ที่ใช้ได้เพื่อความสวยของหน้าที่
  เปิดปีละไม่กี่ครั้ง แผงจึงเป็น popover ที่มีแต่ของที่ต้อง*มอง* ส่วนของที่ต้อง*กด*
  อยู่หลังเฟือง
- **popover เป็น `.applicationDefined` แล้วปิดเองด้วยตัวดักคลิก ไม่ใช่ `.transient`** —
  `.transient` ปิดตัวเองเมื่อได้เหตุการณ์นอกหน้าต่างของมัน ซึ่งรวมถึงตอนเมนูเฟือง
  ที่เด้งจากในตัวมันเองกำลัง track อยู่ แผงจึงหายไปใต้เมนูของตัวเอง แลกโค้ดดักคลิก
  ไม่กี่บรรทัดกับพฤติกรรมที่อธิบายได้
- **การ์ดโควตาสองใบใช้เกณฑ์สีสามขั้นของบอร์ด ไม่ใช่สองสถานะของแถบเมนู** — ที่นี่มีที่ให้
  ไล่สีและผู้ใช้กำลังมองอยู่จริง (เขากดเปิดเอง) `warn_pct`/`crit_pct` จึงกลับมาครบ
  พร้อมกฎ "แซง pace = แดงทันทีแม้ยังไม่ถึง 85" — เกณฑ์เดียวกับ `usage_bar_color`
  ทั้งฝั่ง Python และ firmware · ฝั่ง host *คัดลอก* เกณฑ์ (`warn_pct`/`crit_pct`) จาก
  `tools/layout.toml` ด้วยมือ — ไม่มีตัวสร้าง TOML→Swift และการทำขึ้นมาเพื่อค่าสองค่า
  คือเครื่องจักรที่ต้องดูแลมากกว่าค่าที่มันดูแล คอมเมนต์อ้างชื่อคีย์ต้นทางไว้แทน
  · สูตร pace ย้ายไปอยู่ที่ `UsageReader.elapsedPercent`
  ที่เดียว เพราะแบดจ์กับการ์ดต้องได้ตัวเลขเดียวกันเสมอ สองสูตรที่เขียนแยกกันจะเพี้ยนกันวันหนึ่ง
- **แต่ *สี* บนการ์ดเป็นสีระบบ ไม่ใช่พาเลตต์ของบอร์ด** — พาเลตต์ใน `layout.toml` ถูกจูน
  มาสำหรับ TFT ที่หรี่ไฟหลังอยู่บนโต๊ะในห้องที่มีไฟ สีเดียวกันนั้นบนพื้นดำของ popover
  อ่านเป็นสีขุ่น ไม่ใช่สัญญาณ · `systemGreen/Orange/Red` ยังตามธีมสว่างมืดและการตั้งค่า
  การเข้าถึงให้เอง ซึ่งค่าคงที่ทำไม่ได้ · สิ่งที่ต้องตรงกันคือ *เกณฑ์ที่เลือกสี* ไม่ใช่ค่า RGB
- **ชื่อการ์ดอยู่บรรทัดบน คำอธิบายอยู่บรรทัดล่าง และ weekly ใช้ pill แทนคำอธิบาย** —
  ชื่อกับคำอธิบายในบรรทัดเดียวกันแย่งกันเป็นสิ่งที่อ่านก่อน และบีบเปอร์เซ็นต์จนไม่ชิดขวา
  เวลาชื่อยาว · `All models` + ป้าย `Weekly` เป็นประโยคเดียวอยู่แล้ว บรรทัดที่สาม
  จะพูดซ้ำสิ่งที่ป้ายพูดไปแล้ว สีของ pill จึงคงที่ ไม่ตามระดับการใช้ ด้วยเหตุผลเดียวกับบอร์ด
- **ขีด pace อยู่บนแถบเสมอ ไม่ใช่เฉพาะตอนแซง** — สีตอบว่า "มีปัญหาไหม" ซึ่งเป็นคำตอบ
  มี/ไม่มี ขีดตอบว่า *ห่างแค่ไหน* ซึ่งเป็นสิ่งที่ทำให้ 35% มีความหมาย · บน popover
  วาดขีดด้วยสีข้อความทับลงไปตรงๆ ได้ ไม่ต้องเจาะร่องโปร่งอย่างบนแถบเมนู เพราะที่นี่
  พื้นหลังเป็นพื้นทึบของแผง ไม่ใช่วอลเปเปอร์ของผู้ใช้
- **เวลารีเซ็ตบอกทั้งสัมพัทธ์และสัมบูรณ์ — `Resets in 2h24m (Today 23:00)`** — อันแรก
  ตอบ "อีกนานไหม" อันหลังตอบ "ตอนนั้นคือเมื่อไรของวัน" ซึ่งเป็นอันที่วางแผนงานต่อได้
  weekly รีเซ็ตได้ไกลถึงเจ็ดวัน `Today/Tomorrow` จึงไม่พอ ต้องมีชื่อวันและวันที่ต่อท้าย
- **บรรทัดอายุของค่ามีหน่วยวินาที ต่างจากที่อื่นในแอป** — คำถามที่ทำให้ฟีเจอร์นี้เกิดคือ
  "เลขนี้ค้างหรือเปล่า" คำตอบที่หยาบระดับนาทีก็แค่ย้ายความสงสัยจากบอร์ดมาไว้บนเมนูบาร์
  และตัวเลขที่เดินทุกวินาทีเป็นหลักฐานว่าแผงยังมีชีวิต บรรทัดนี้อยู่ *ใต้การ์ด* ไม่ใช่ท้ายแผง
  เพราะเป็นคำอธิบายของตัวเลขที่อยู่เหนือมัน ไม่ใช่สถานะของแอปอย่างบรรทัดบอร์ด
- **แผงที่เปิดค้างมีนาฬิกาของตัวเองที่หยุดตอนปิด** — countdown กับอายุของค่าเดินตลอดเวลา
  แต่ไม่มีใครเห็นตอนแผงปิด อ่าน cache จากดิสก์ทุกวินาทีเพื่อข้อความที่ไม่มีใครมองคือเผาแบต
  timer ถูกหยุดที่ `popoverDidClose` ที่เดียว ด้วยเหตุผลเดียวกับตัวดักคลิก — ทางปิดมีหลายทาง
- **ไม่มีค่าเลยทั้งสองหน้าต่าง → ซ่อนการ์ดทั้งช่อง เหลือแต่บรรทัดอายุ** — การ์ดเปล่าสองใบ
  อ่านได้ว่าอุปกรณ์พัง หลักเดียวกับ "ไม่รู้โควตา → ไอคอนเดิม" บนแถบเมนู · หน้าต่างที่รู้
  ใบเดียวยังโชว์ทั้งคู่ โดยใบที่ไม่รู้เขียน `--%` สีจาง ไม่ใช่ `0%` สีเขียว
- **ปุ่ม refresh เป็นทางออกฉุกเฉิน ไม่ใช่ทางปกติ — ยิงหนึ่งรอบ แล้วเย็นตัว 10 วินาที** —
  endpoint นี้ไม่มีเอกสาร และเราตัดตัวเลือก 30 วินาทีทิ้งไปเพราะไม่อยากยิงถี่เกินจำเป็น
  ปุ่มที่กดรัวได้จะทำลายเหตุผลนั้นทั้งหมด · ทางปกติคือ*การเปิดแผง*เอง ซึ่งเป็นสัญญาณ
  ความตั้งใจที่ชัดพอจะใช้เป็น trigger อยู่แล้ว — เปิดแล้วยิงให้เมื่อค่าที่มีเก่ากว่ารอบที่ตั้งไว้
  (`RefreshControl.wantsPoll`) ผู้ใช้จึงไม่ต้องกดปุ่มเพื่อบอกสิ่งที่เขาบอกไปแล้วด้วยการเปิด
  · การเย็นตัวนับจากตอน *จบ* ไม่ใช่ตอนเริ่ม ไม่งั้นรอบที่ช้ากว่าสิบวินาทีจะไม่มีการเย็นตัวเลย
  · เกณฑ์ตอนเปิดดูสองอย่าง ไม่ใช่อย่างเดียว: ค่าล่าสุดที่*กลับมา* (`UsageReader.stamp`)
  กับรอบล่าสุดที่*ออกไป* (`UsagePoller.lastStarted`) — รอบที่ล้มเหลวไม่เคยขยับอันแรก
  ถ้าดูแต่อันแรก การเปิดปิดแผงตอนเน็ตล่มจะยิงลูกทุกครั้งที่ชำเลืองดู
- **`Off` และ key ที่หมดอายุไม่ปิดปุ่ม** — `Off` สั่งว่าแอปอย่ายิง*เอง* ไม่ได้สั่งว่าห้าม
  ผู้ใช้ดูค่าใหม่ · ล็อก key หมดอายุมีไว้กันการยิงซ้ำทุกนาทีเพื่อรับ 401 เดิม ซึ่งไม่ใช่เหตุผล
  ที่จะปฏิเสธคนที่เพิ่งไปเอา key ใหม่มาแปะข้างนอกแล้วอยากรู้ว่าใช้ได้หรือยัง (ถ้ายังหมดอายุจริง
  รอบนั้นก็ตอบแบบเดิม) `UsagePoller` จึงมีสองประตู: `pollNow` (นาฬิกา เคารพทุกกฎ) กับ
  `refreshNow` (คน ข้าม `Off` และข้ามล็อก) เหลือแค่ "ลูกวิ่งอยู่แล้ว" กับ "ไม่มีไฟล์ key"
  ที่ยังกันทั้งคู่ — อันหลังไม่มีคำถามจะยิงตั้งแต่แรก
- **การเย็นตัวเป็นวินัยของ*ปุ่ม* ไม่ใช่ของ poller** — ถ้านับรอบอัตโนมัติด้วย ที่ 60 วินาที
  ปุ่มจะกดไม่ได้หนึ่งในหกของเวลาทั้งหมดโดยไม่มีใครเข้าใจว่าทำไม `MenuBarApp` จึงจำไว้เอง
  ว่ารอบไหนเป็นของปุ่ม และตั้งค่านั้นจาก `isRunning` ไม่ใช่ตั้ง `true` ดื้อๆ — รอบที่ไม่ได้
  เริ่มจริงจะทำให้ปุ่มค้างรอรอบที่ไม่มีวันจบ
- **กำลังยิง = ตัวหมุนแทนที่ปุ่ม ไม่ใช่ปุ่มที่จางลง** — ปุ่มจางอ่านได้ว่า "กดไม่ได้ตอนนี้"
  ซึ่งเป็นภาพเดียวกับตอนเย็นตัว ทั้งที่สองอย่างนี้ต่างกัน · ทั้งคู่ยืนในกรอบขนาดคงที่
  ไม่งั้นหัวแผงขยับทุกครั้งที่เริ่มและจบการยิง
- **ตัวสลับ org ย้ายมาอยู่หลังลูกศรข้างชื่อที่หัวแผง และหายไปจากเมนูเฟือง** — ชื่อ org
  ที่ใช้อยู่คือหัวของแผงอยู่แล้ว ที่สลับจึงควรอยู่ตรงนั้น ที่สลับสองที่แปลว่าผู้ใช้ต้องจำว่า
  อันไหนคืออันจริง · ลูกศรโผล่เมื่อมีมากกว่าหนึ่ง org เท่านั้น — ลูกศรที่กดแล้วเจอรายการ
  ที่มีตัวเลือกเดียวคือคำสัญญาที่ผิด · ยังไม่ได้ตั้ง key = ยังไม่เคยถามว่าบัญชีมี org อะไรบ้าง
  หัวแผงจึงเป็นชื่อแอป ไม่ใช่ชื่อที่ค้างมาจาก key ตัวก่อน
- **แผงต้องถมไฮไลต์ให้ปุ่มบนแถบเมนูเอง** — การถมมาฟรีกับ `statusItem.menu` เท่านั้น
  ปุ่มที่เด้ง popover ดูเหมือนไม่ได้ถูกกดถ้าไม่บอก และแบดจ์ยังต้องวาดใหม่ตอนถูกถม
  ด้วยเหตุผลเดิม (ภาพที่ไม่ใช่ template อ่านไม่ออกบนพื้นสีเน้น)
- **ลายเซ็นเป็น adhoc** พอสำหรับเครื่องที่บิลด์เอง แต่สิทธิ์ TCC ผูกกับ cdhash
  บิลด์ใหม่ = ถามสิทธิ์ Bluetooth ใหม่ การแจกให้เครื่องอื่นต้องมี Developer ID + notarization

**ยังไม่ได้ทำ**: เก็บสถานะข้ามการรีสตาร์ท daemon · ปรับ session timeout จากเมนู ·
แจกแบบ notarized (ต้องมีบัญชีนักพัฒนา)

## ฝั่ง firmware (ESP-IDF v5.5)

```
firmware/main/ct_lcd.c      SPI + init ILI9341 ตามผลวัด + ไฟหลัง PWM (LEDC)
firmware/main/ct_rects.c    rect list ในพิกัด unit + ขอบ + กรอบรวม
firmware/main/ct_props.c    prop ทั้ง 9 ชิ้น  (พอร์ตตรงจาก tools/gen/props.py)
firmware/main/ct_mascot.c   mood + สถานะ + การจัดกึ่งกลาง (พอร์ตจาก mascot.py)
firmware/main/ct_model.c    JSON snapshot -> struct (cJSON)
firmware/main/ct_ui.c       หน้าจอทั้งใบด้วย LVGL 9.2 (โครงเดียวกับ screen.py)
firmware/main/ct_ble.c      NimBLE GATT server 3 characteristic
firmware/main/ct_led.c      RGB LED กะพริบตอนมีการเตือนใหม่
firmware/main/main.c        ประกอบทุกอย่าง + ลูปหลัก
```

`idf.py -p /dev/cu.usbserial-XX flash monitor` — บิลด์ได้ ~900KB ใน partition 3MB

**สิ่งที่ตัดสินใจเพิ่มตอนเขียน firmware**

- **มาสคอตวาดด้วย `LV_EVENT_DRAW_MAIN` ต่อ slot** ไม่ใช่ canvas — canvas เต็มจอกิน 150KB
  ซึ่งบอร์ดไม่มีให้ ส่วน draw callback วาดลงบัฟเฟอร์บางส่วนของ LVGL โดยตรง
- **ฉากท้องฟ้าเป็น object ผืนเดียวที่สร้างก่อนใครเพื่อน** — LVGL เรียงชั้นตามลำดับสร้าง
  เมฆอยู่ในผืนเดียวกัน ไม่ใช่ object แยกที่เลื่อนด้วย `lv_obj_set_pos`: แถบฟ้า 320x71
  = 22,720 px ซึ่งน้อยกว่าที่ 3 slot วาดใหม่ทุกเฟรมอยู่แล้ว (28,620 px) จึงไม่ต้องแลก
  ความซับซ้อนกับอะไรเลย
- **ฟ้าวาดใหม่เมื่อภาพเปลี่ยนจริง ไม่ใช่ทุกเฟรม** — เมฆขยับ 4 px/วิ จึงเปลี่ยนภาพ 4 ครั้ง/วิ
  ส่วนลูปหลักเดิน 16 เฟรม/วิ · ใช้ `lv_obj_invalidate_area` เฉพาะแถบฟ้า พื้นดินกับหญ้า
  นิ่งตลอดช่วง จะวาดใหม่ทั้งผืนก็ต่อเมื่อช่วงเวลาเปลี่ยน (นาทีละครั้งเป็นอย่างมาก)
- **ตารางดาว/หญ้า/เมฆ generate เป็น C array จาก `layout.toml`** — ถ้าปล่อยให้แต่ละฝั่ง
  สุ่มตำแหน่งเอง ภาพบน preview จะไม่ใช่ภาพบนบอร์ด แล้ว preview ก็หมดประโยชน์
- **เงาใต้เท้าวาดเป็นแคปซูล ไม่ใช่วงรี** — `lv_draw_rect` clamp radius ที่ครึ่งด้านสั้น
  ฝั่ง preview จึงต้องใช้ `rounded_rectangle` ตาม ไม่ใช่ `ellipse` ที่สวยกว่าแต่คนละรูป
- **ปิด `LV_USE_TEXTAREA` ไม่ได้** — spinbox ใน LVGL 9.2 พึ่งมันแบบ `#error`
- **`ct_model_parse` เขียนทับ snapshot เดิมทีเดียวตอนท้าย** — JSON พังกลางทางต้องไม่ทิ้งภาพครึ่งๆ
- **สถานะที่ firmware ไม่รู้จักตกเป็น `idle`** ไม่ใช่ค้างจอ — daemon อาจใหม่กว่า firmware
- **ชื่ออุปกรณ์ไปอยู่ใน scan response** ส่วน adv บรรจุ service UUID 128 บิต
  ทั้งคู่ไม่พอใน 31 ไบต์ และฝั่ง Mac สแกนด้วย UUID จึงต้องเป็นตัวที่อยู่ใน adv
- **daemon ต้องรันจาก `.app` bundle จริง ไม่ใช่ไบนารีเปล่า** — TCC บน macOS 26
  ไม่ยอมรับ `__info_plist` ที่ฝังในไบนารี Mach-O เปล่า ผลคือ CoreBluetooth เงียบสนิท
  (ไม่มี callback ไม่มี error) และพอเรียกจาก Terminal ที่มีสิทธิ์ถามได้จริง
  มันยิง SIGABRT ทิ้งทั้งโปรเซส (`__TCC_CRASHING_DUE_TO_PRIVACY_VIOLATION__`)
  ประกอบด้วย `host/Scripts/make-app.sh` ลายเซ็น adhoc ผูกสิทธิ์กับ cdhash
  จึงต้องกดอนุญาตใหม่ทุกครั้งที่บิลด์ใหม่

**ยืนยันบนบอร์ดจริงแล้ว**: บูตผ่าน จอ init สำเร็จ NimBLE ขึ้น ไม่มี watchdog
**ยังไม่ได้ยืนยัน**: ภาพบนจอ (ต้องมองด้วยตา) · BLE จับคู่กับ daemon จริง (ติดสิทธิ์ Bluetooth
ของ process ที่รัน daemon — ต้องรันจาก Terminal ที่ผู้ใช้อนุญาตได้) · RGB LED

## เครื่องมือฝั่ง Python

```
tools/layout.toml          ต้นทางเดียวของค่าคงที่ layout + จานสี
tools/gen/rects.py         ชนิดข้อมูล rect list + การแปลง (เลื่อน/สะท้อน/ย่อ/ทำขอบ)
tools/gen/mascot.py        โครงร่างมาสคอต, mood, ตาราง state -> (mood, prop)
tools/gen/props.py         prop ทั้ง 9 ชิ้น + กรอบวาดมาตรฐาน
tools/gen/screen.py        ประกอบจอ 320x240 ทั้งใบ
tools/gen/render.py        rect list -> ภาพ (บีบสีเป็น RGB565 ให้ตรงกับจอจริง)
tools/preview.py           สร้าง out/states.png, out/anim/*.gif, out/screen_*.png|gif
tools/export_layout.py     layout.toml -> firmware/main/layout.h
```

