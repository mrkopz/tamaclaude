// หน้าหุ้น — โครงเดียวกับ tools/gen/stocks.py
//
// ที่นี่วาดอย่างเดียว: page frame ที่วาดและนาฬิกาทุกเรือนเป็นของ `ct_pages` (ADR-0002)
//
// ยืมรูปหน้าคริปโตมาทั้งใบ รวมทั้งลูกศรและสีซึ่ง *เรียกของหน้านั้นมาใช้ตรงๆ* ไม่ได้ลอกมา:
// กติกา "ขึ้นกับลงต้องแยกออกโดยไม่อ่านตัวเลข" ต้องมีสำเนาเดียว ไม่งั้นวันหนึ่งลูกศรของ
// สองหน้าจะชี้คนละแบบ ต่างกันข้อเดียวที่มองเห็น: บรรทัดล่างบอกได้ว่าตลาดปิด
#pragma once

#include "ct_stocks.h"
#include "lvgl.h"

// สร้าง widget ทั้งหมดใต้ `parent` แล้วผูกกับสิ่งที่จะวาด
//
// `has_frame` คือธงของตัวโฮสต์ที่บอกว่าเคยได้ข้อมูลของหน้านี้แล้วหรือยัง — สภาพ
// "ยังไม่เคยได้เลย" มีหน้าตาของตัวเอง ห้ามเป็นจอเปล่าหรือโครงว่าง (ADR-0002)
void ct_stocks_ui_init(lv_obj_t *parent, const ct_stocks_t *frame, const bool *has_frame);

// เฟรมที่ผูกไว้เปลี่ยนไปแล้ว — วาดใหม่ทั้งใบ
void ct_stocks_ui_redraw(void);

// ตัวโฮสต์เพิ่งเดินอายุข้อมูลไปหนึ่งวินาที — วาดเฉพาะบรรทัดที่บอกอายุ
void ct_stocks_ui_redraw_age(void);

// มี snapshot สดอยู่ไหม — หลุดแล้วตัวเลขที่ค้างอยู่เป็นเทา เหมือนที่หน้าอื่นทำ
void ct_stocks_ui_set_connected(bool connected);
