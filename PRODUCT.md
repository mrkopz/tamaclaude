# Product

<!-- impeccable:product-schema 1 -->

## Platform

embedded

ไม่ใช่ `web` / `ios` / `android` / `adaptive` — ผลิตภัณฑ์นี้มีสองพื้นผิว: จอ ILI9341
320x240 บนบอร์ด ESP32 (LVGL v9, C) และแอป AppKit บน macOS 14+ ไม่มี native mobile
reference ใดที่ใช้ได้ อย่าโหลด `ios.md` หรือ `android.md` กับโปรเจกต์นี้

## Users

นักพัฒนาที่ใช้ Claude Code ทั่วไป — ไม่ใช่แค่ผู้เขียนโปรเจกต์นี้ คนกลุ่มนี้สั่ง agent ให้ทำงาน
แล้วละสายตาไปทำอย่างอื่น (อ่านเอกสาร ประชุม พักดื่มน้ำ) โดยที่ยังอยากรู้ว่า session
ยังเดินอยู่ไหม ติดรออนุมัติหรือเปล่า หรือจบไปแล้ว

งานที่เขาทำตอนมองจอนี้คือ **การเหลือบ ไม่ใช่การอ่าน** จอตั้งอยู่ข้างคีย์บอร์ด นอกโฟกัสสายตาหลัก
คำตอบต้องมาถึงในสายตาเดียวโดยไม่ต้องหยุดสิ่งที่กำลังทำอยู่ ถ้าต้องเพ่งเพื่อจะเข้าใจ = พังแล้ว

ผู้ใช้ต้องประกอบเองได้ตั้งแต่ซื้อบอร์ด แฟลชเฟิร์มแวร์ ไปจนตั้งค่า — แต่การซื้อบอร์ดมาแล้ว
ไม่ได้แปลว่าอยากตั้งค่าอะไรอีกทุกวัน ตั้งครั้งเดียวแล้วลืมมันไป

## Product Purpose

เปลี่ยนสถานะของ Claude Code session จากสิ่งที่ต้องไป *หา* ในหน้าต่างเทอร์มินัล เป็นสิ่งที่
*อยู่ตรงนั้นอยู่แล้ว* บนโต๊ะ — และเมื่อไม่มี session เดินอยู่ ก็ยังคุ้มพื้นที่บนโต๊ะด้วยหน้าอื่น
(อากาศ คริปโต หุ้น ปฏิทิน)

สำเร็จ = ผู้ใช้เลิกสลับหน้าต่างไปเช็กว่า agent ทำเสร็จหรือยัง และไม่รู้สึกอยากเก็บอุปกรณ์ลงลิ้นชัก
ในสัปดาห์ที่สอง

## Positioning

จอสถานะทั่วไปแสดง *ตัวเลข* ตัวนี้แสดง *ตัวละคร* — มาสคอต Claude สื่อสองมิติพร้อมกัน:
props บอกว่ากำลังใช้เครื่องมืออะไร ตา/ลำตัวบอกอารมณ์ของสถานะ อ่านได้จากมุมสายตาโดยไม่ต้องแปล

สิ่งที่ผลิตภัณฑ์ข้างเคียงลอกไม่ได้จริงๆ คือ **โครงสร้างของความจริง**: daemon เป็นเจ้าของทุกการ
ตัดสินใจเรื่องเนื้อหา บอร์ดเป็นเจ้าของว่าหน้าไหนอยู่บนจอตอนนี้ การแบ่งแบบนี้ทำให้ Mac ที่หลับ
ไปไม่สามารถแช่จอไว้ที่หน้าเดียวทั้งคืนได้ — จอยังหมุนหน้า ยังปัดได้ และยังบอกตรงๆ ว่าลิงก์หลุด

## Operating Context

- อุปกรณ์ตั้งอยู่บนโต๊ะทำงานถาวร เสียบไฟตลอด ไม่มีปุ่ม ไม่มีใครมากดรีเซ็ต
- แสงห้องเปลี่ยนตลอดวัน แต่ **ความสว่างจอคงที่** ไม่หรี่อัตโนมัติ (ตัดสินใจแล้ว)
- อินพุตเดียวคือการปัดซ้าย/ขวาเปลี่ยนหน้า ไม่มี tap ไม่มีโซนแตะ ไม่มีลากแนวตั้ง (ADR-0007)
- หน้าหมุนเองตามรอบเมื่อไม่มีใครแตะ และกระโดดกลับหน้ามาสคอตเมื่อมี attention event
- Mac หลับ = จอค้าง ทั้ง BLE และ LAN เงียบพร้อมกัน อุปกรณ์บอกสถานะนี้ตรงๆ ไม่แกล้งทำเป็นสด
- ระยะการอ่านคือระยะแขน มุมมองเฉียง ไม่ใช่ตั้งฉาก

## Capabilities and Constraints

**หน้าที่มี**: มาสคอต (สถานะ session) · weather · crypto · stocks · calendar
`PageKind` เป็นสัญญาสามทางระหว่าง Swift / C / Python — ค่าดิบเดินทางบนสาย

**ข้อจำกัดที่เปลี่ยนไม่ได้**:

- จอ 320x240 พิกเซล 16-bit สี ไม่มี anti-aliasing ที่ไว้ใจได้ เส้นบางหายจริง
- หนึ่งเฟรมต่อหนึ่งหน้า และแต่ละเฟรมต้องพอดี MTU 500 ไบต์เดี่ยวๆ (ADR-0003)
  `Snapshot.encoded` บีบเนื้อ → หัวเรื่อง → ตัดการ์ดทิ้ง แต่ไม่แตะจำนวน session
- Asset ทุกชิ้นเป็น rect list `{x,y,w,h,color}` generate จาก Python — ไม่มีบิตแมป
  ไม่มี sprite pipeline ฟอนต์บนบอร์ดมี charset จำกัด (ไม่มี em dash) ไทยผ่านได้เฉพาะที่
  shape แล้วผ่าน `Text.fit` (ADR-0008)
- ค่าเลย์เอาต์ทุกตัวมาจาก `tools/layout.toml` ที่เดียว ห้ามแก้ `layout.h`
- ทุกท่าอยู่อย่างน้อย 5 วินาที ท่าที่ข้ามระหว่างนั้นถูกทิ้ง ไม่เข้าคิว
- การเปลี่ยนภาพหมายถึงแก้สองฝั่งเสมอ: `tools/gen/*.py` และ `firmware/main/ct_*.c`

**ยังไม่ตัดสิน**: เก็บสถานะข้ามการรีสตาร์ท daemon · ปรับ session timeout จากเมนู ·
แจกแบบ notarized

## Brand Commitments

- **มาสคอต Claude สีส้ม `#D97757` ทรงบล็อคสี่เหลี่ยม** — ผู้ใช้ยืนยันว่าแตะไม่ได้ อัตลักษณ์
  ทั้งหมดอยู่ตรงนี้
- ความเห็นและเอกสารออกแบบเขียนภาษาไทย · identifier คำสั่ง และ error string เป็นอังกฤษ

## Evidence on Hand

- `DESIGN.md` — ข้อตกลงปัจจุบันและเหตุผลที่โค้ดบอกไม่ได้ (ไทย) เป็น visual authority ที่มีอยู่จริง
- `docs/adr/` — การตัดสินใจที่กลับคำแล้วมีบันทึกไว้
- `tools/preview.py` → `out/` — เรนเดอร์จอทั้งใบเป็น PNG ไม่มี simulator SDL2
  นี่คือลูป dev เพียงทางเดียวสำหรับงานภาพ
- `tools/preview.py --limits` — ค่าที่ *วัดจริง* ว่าป้ายแต่ละอันจุกี่ตัวอักษร ไม่ใช่ค่าที่เดา
- `firmware/probe/` — ค่าจริงของแผงจอ (MADCTL, colour order, inversion)
- `docs/images/tamaclaude-logo.png` — โลโก้วาดมือ สำหรับ icon ≥128px เท่านั้น

**ไม่มี**: ผู้ใช้จริงนอกผู้เขียน · ข้อมูลการใช้งาน · เทสต์กับคน · ยอดติดตั้ง
อย่าแต่งขึ้นมา

## Product Principles

1. **เหลือบเดียวต้องพอ** ทุกอย่างบนจอถูกตัดสินด้วยว่ามันอ่านได้จากหางตาหรือไม่ ไม่ใช่ว่ามัน
   สวยตอนซูมดู
2. **ตัวละครมาก่อนตัวเลข** สถานะที่สื่อผ่านท่าทางชนะสถานะที่สื่อผ่านข้อความทุกครั้งที่เลือกได้
3. **ไม่แกล้งทำเป็นสด** ข้อมูลเก่าต้องดูเก่า ลิงก์หลุดต้องเห็นว่าหลุด หน้าที่ถูกปิดต้องหายไป
   ไม่ใช่ค้างตัวเลขเมื่อวานหมุนต่อทั้งคืน
4. **ข้อจำกัดคือรูปทรง** 500 ไบต์ 320x240 และ rect list ไม่ใช่สิ่งที่ต้องหลบเลี่ยง —
   ภาษาภาพบล็อคๆ ของงานนี้เกิดจากมันโดยตรง
5. **ความจริงหนึ่งชุดต่อหนึ่งเรื่อง** ค่าเดียวกันอยู่สองที่เมื่อไร มันจะต่างกันเมื่อนั้น

## Accessibility & Inclusion

จอไม่มี text alternative และไม่มีเสียง — ข้อมูลทุกอย่างเป็นภาพล้วน ดังนั้น:

- **ห้ามใช้สีเป็นตัวส่งข้อมูลเพียงอย่างเดียว** สถานะเตือนต้องต่างที่รูปทรง/ตำแหน่ง/ท่าทางด้วย
  ไม่ใช่แค่แดงกับเขียว
- คอนทราสต์ต้องรอดที่มุมมองเฉียงและระยะแขน ไม่ใช่แค่ผ่านตอนดูตรงๆ บนจอมอนิเตอร์
- ฝั่ง macOS ยังไม่ได้กำหนดมาตรฐาน a11y ไว้ชัด — ยังไม่ตัดสิน
